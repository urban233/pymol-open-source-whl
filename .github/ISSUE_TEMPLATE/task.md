---
name: Task
about: One bounded unit of work, ready to build
title: ""
labels: task
---

<!-- Design doc / brief / delivery-plan link. Required if one exists for
     this project. -->
**Design doc:**

<!-- One or two sentences: what will exist when this is done that does not
     exist now. Not why, and not how -- the design doc or brief owns those. -->

## Acceptance criteria

<!-- Executable checks. Each line is a command and its expected result,
     runnable by someone who did not write the code. "Tests pass" is not an
     acceptance criterion. Evidence that they were actually run belongs in
     the pull request, not here. -->

- [ ] `` ->
- [ ] `` ->

## Stop if

<!-- The condition under which you halt and escalate to a human rather than
     push through. If you cannot name one, you do not yet know where this
     is fragile. Link the design doc's open question it would raise, if
     there is one. -->

## Assumptions

<!-- Facts this relies on that are not yet confirmed against the current
     repository. Mark each with the inline `[unverified]` tag at the point
     it is used, in the code and docs it affects, not only here -- and treat
     confirming it as part of the acceptance criteria. Delete this section
     if there are none; do not write "none". -->

<!-- ─────────────────────────────────────────────────────────────────────
     If this project tracks a delivery plan, the title may keep that
     plan's row ID (e.g. "W-01: ...") so the two stay traceable to each
     other -- optional, since not every task has one.

     Set as labels: risk:low|normal|high|critical (matches
     implementation-plan.template.md's Focus card scale)
     Set as fields: milestone, assignee, blocked-by via linked issues
     ───────────────────────────────────────────────────────────────────── -->
